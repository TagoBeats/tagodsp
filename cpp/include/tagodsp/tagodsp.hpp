#pragma once

// tagodsp: header-only C++ core.
// Modules are added here only after full validation in the Python workbench.

namespace tagodsp {

inline constexpr int versionMajor = 0;
inline constexpr int versionMinor = 1;
inline constexpr int versionPatch = 0;

} // namespace tagodsp
